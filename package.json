{
  "name": "@oresoftware/ores-middleware-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "contracts:compile": "tsp compile contracts/typespec --output-dir target/contracts/typespec && tsp compile contracts/docs-serving.tsp --no-emit && tsp compile contracts/persistence/idempotency-record.tsp --no-emit",
    "contracts:compare": "node scripts/compare-authorities.mjs && python3 scripts/check_contract_parity.py && python3 scripts/schema_convergence.py",
    "contracts:validate": "node scripts/validate-contracts.mjs",
    "contracts:check": "npm run contracts:compare && npm run contracts:validate",
    "descriptors:check": "node scripts/check-descriptors.mjs --require-all",
    "zpkg:check": "python3 scripts/check_zpkg.py",
    "audit": "python3 scripts/audit.py --receipt target/audit/receipt.json",
    "verify": "npm run contracts:compile && npm run contracts:check && npm run zpkg:check && node scripts/check-source-layout.mjs"
  },
  "devDependencies": {
    "@typespec/compiler": "^1.0.0",
    "@typespec/http": "^1.0.0",
    "@typespec/json-schema": "^1.0.0",
    "@typespec/openapi3": "^1.0.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
