import type { LogRecord, LogTransport } from '../base-logger.js';
import type { SupabaseIngestOptions, SupabaseIngestSnapshot } from './types.js';
export declare class SupabaseIngestTransport implements LogTransport {
    readonly name = "supabase-ingest";
    readonly options: Readonly<SupabaseIngestOptions>;
    readonly endpoint: string;
    private readonly queue;
    private readonly publishableKey;
    private pendingBatch;
    private flushPromise;
    private intervalTimer;
    private retryTimer;
    private retryAttempts;
    private acknowledged;
    private rejected;
    private dropped;
    private failures;
    private retries;
    private accepting;
    private closed;
    private closePromise;
    constructor(options: SupabaseIngestOptions);
    snapshot(): SupabaseIngestSnapshot;
    private hasData;
    private drop;
    private reportError;
    private enqueue;
    private createBatch;
    private scheduleInterval;
    private now;
    private randomUnit;
    private scheduleRetry;
    write(record: LogRecord): Promise<void>;
    private batchBody;
    private post;
    private clearTimers;
    private rejectPending;
    private acknowledge;
    private drain;
    flush(): Promise<void>;
    flushOnExit(): Promise<void>;
    close(): Promise<void>;
    private performClose;
}
export declare function createSupabaseIngestTransport(options: SupabaseIngestOptions): SupabaseIngestTransport;
//# sourceMappingURL=transport.d.ts.map