import type { Linter, Rule } from 'eslint';
export interface RequireSendRuleOptions {
    /** Extra variable or property paths that hold logger instances. */
    loggerNames?: string[];
    /** Extra package names that export next-loggers-compatible factories. */
    moduleNames?: string[];
}
export declare const requireSendRule: Rule.RuleModule;
export declare const rules: {
    'require-send': Rule.RuleModule;
};
export declare const eslintPlugin: {
    meta: {
        name: string;
        version: string;
    };
    rules: typeof rules;
    configs: Record<string, Linter.Config>;
};
export default eslintPlugin;
//# sourceMappingURL=eslint-plugin.d.ts.map