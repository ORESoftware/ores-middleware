import type { LogRecord, LogTransport } from './base-logger.js';
import { type SupabaseRealtimeAckOptions, type SupabaseRealtimeSnapshot } from './supabase-realtime-common.js';
export * from './supabase-realtime-common.js';
/**
 * Private Supabase Broadcast transport that waits for the exact Phoenix reply.
 * An acknowledgement proves Realtime receipt, not a durable Postgres commit.
 */
export declare class SupabaseRealtimeAckTransport implements LogTransport {
    readonly name = "supabase-realtime-ack";
    readonly options: Readonly<SupabaseRealtimeAckOptions>;
    private queue;
    private readonly inFlight;
    private readonly idleWaiters;
    private socket;
    private connectTask;
    private reconnectTimer;
    private heartbeatTimer;
    private heartbeatAckTimer;
    private heartbeatRef;
    private joinRef;
    private ref;
    private joined;
    private accepting;
    private closed;
    private closeTask;
    private reconnectAttempts;
    private acknowledged;
    private failures;
    private dropped;
    constructor(options: SupabaseRealtimeAckOptions);
    snapshot(): SupabaseRealtimeSnapshot;
    private get topic();
    private nextRef;
    private hasAwaiter;
    private error;
    private drop;
    private notifyIdle;
    private waitForIdle;
    private stopHeartbeat;
    private heartbeat;
    private startHeartbeat;
    private closeSocket;
    private restoreInFlight;
    private failPending;
    private randomUnit;
    private scheduleReconnect;
    private socketFactory;
    private token;
    private open;
    private connect;
    private onMessage;
    private retry;
    private pump;
    private kick;
    write(record: LogRecord): Promise<void>;
    flush(): Promise<void>;
    flushOnExit(_records?: readonly LogRecord[]): Promise<void>;
    refreshAuth(): void;
    close(): Promise<void>;
    private performClose;
}
export declare const createSupabaseRealtimeAckTransport: (options: SupabaseRealtimeAckOptions) => SupabaseRealtimeAckTransport;
//# sourceMappingURL=supabase-realtime.d.ts.map