[package]
name = "ores-middleware-docs"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "MIT"
description = "Routing-neutral ORES API documentation middleware decision core"
repository = "https://github.com/ORESoftware/ores-middleware"

[lib]
name = "ores_middleware_docs"
path = "../docs-serving-lib/lib.rs"

[dependencies]

[lints.rust]
unsafe_code = "forbid"

# Keep this nested crate independent of the root SDK workspace. Its source lives
# beside it so the shared conformance fixture remains addressable from one file.
[workspace]
